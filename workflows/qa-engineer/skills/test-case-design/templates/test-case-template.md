# 测试用例模板

## 用例标题

`[模块]-[功能]-[场景]`，例如：`订单-创建-超出库存上限报错`

---

## 1. 用例信息

```text
用例 ID：TC-[模块]-[序号]
优先级：P0 / P1 / P2 / P3
类型：正向 / 反向 / 边界 / 状态 / 权限 / 性能
来源：PRD#X.Y / Bug#123 / 探索式 charter / 历史回归
设计方法：等价类 / 边界值 / 决策表 / 状态转换 / 错误推测
是否可自动化：是 / 否
预估执行时间：X 分钟
```

---

## 2. 前置条件

```text
账号：role=user, tenant=A
数据：已存在订单 ID=xxx, 状态=draft
环境：staging
依赖：第三方支付 Mock 已配置
```

---

## 3. 测试步骤

| # | 操作 | 数据 | 预期结果 |
|---|------|------|---------|
| 1 | 登录 user A | username/password | 进入首页 |
| 2 | 打开订单 xxx | - | 看到状态 draft |
| 3 | 点击"提交" | - | 状态变为 submitted, 触发 webhook |

---

## 4. 后置条件

```text
数据状态：订单 xxx 状态=submitted
回滚需要：将订单状态重置为 draft
影响：发送了一封确认邮件（Mock）
```

---

## 5. 异常路径覆盖

| 异常 | 输入 / 操作 | 预期结果 |
|---|---|---|
| 库存不足 |  | 返回 INSUFFICIENT_STOCK，状态保持 draft |
| 网络超时 |  | 返回 TIMEOUT，可重试，无重复扣款 |
| 权限不足 |  | 返回 403，无变更 |
| 并发提交 |  | 仅一次成功，第二次返回 409 |

---

## 6. 验证点

```text
□ HTTP 状态码符合预期
□ 响应字段符合 OpenAPI Schema
□ 数据库状态变更符合预期
□ Webhook / 异步任务触发
□ 日志包含 trace_id
□ UI 展示符合设计稿
□ 无敏感字段泄露
```

---

## 7. 备注

```text
关联需求：PRD#3.2
关联 Bug：BUG-456
关联自动化：tests/e2e/order_submit.spec.ts
最后执行：[日期] by [负责人] → 结果：通过 / 失败
```
