# 性能基线 / 压测报告模板

## 1. 测试信息

```text
测试目标：[模块/端点]
测试类型：负载 / 压力 / 浸泡 / 尖峰
计划日期：
执行日期：
环境：staging / pre-prod
版本：vX.Y.Z
执行人：
```

---

## 2. SLO / 验收标准

```text
P99 响应时间：< X ms
P95 响应时间：< X ms
错误率：< X%
QPS 目标：>= X
并发：>= X 用户
持续时间：X 分钟
```

---

## 3. 测试场景

### 场景 1：[名称]

```text
模型：恒定负载 / 阶梯升压 / 尖峰冲击
QPS：从 X 到 Y
持续：X 分钟
用户分布：X% 角色 A + Y% 角色 B
数据规模：X 条订单 + Y 条用户
```

---

## 4. 测试脚本（k6 示例）

```javascript
import http from 'k6/http';
import { check } from 'k6';

export const options = {
  stages: [
    { duration: '5m', target: 100 },
    { duration: '10m', target: 100 },
    { duration: '2m', target: 0 },
  ],
  thresholds: {
    http_req_duration: ['p(99)<500'],
    http_req_failed: ['rate<0.01'],
  },
};

export default function () {
  const res = http.get('https://api.example.com/v1/orders');
  check(res, { 'status 200': (r) => r.status === 200 });
}
```

---

## 5. 结果数据

### 响应时间

| 指标 | 目标 | 实测 | 是否达标 |
|---|---|---|---|
| P50 | < 100ms |  |  |
| P95 | < 300ms |  |  |
| P99 | < 500ms |  |  |
| Max |  |  |  |

### 吞吐量

| 指标 | 目标 | 实测 | 是否达标 |
|---|---|---|---|
| QPS | >= 1000 |  |  |
| 总请求数 |  |  |  |
| 错误数 |  |  |  |
| 错误率 | < 1% |  |  |

### 资源使用（USE Method）

| 资源 | Utilization | Saturation | Errors |
|---|---|---|---|
| CPU |  |  |  |
| Memory |  |  |  |
| Disk I/O |  |  |  |
| Network |  |  |  |
| 数据库连接池 |  |  |  |

---

## 6. 瓶颈分析

```text
瓶颈点：[具体位置，如 数据库 / 缓存 / 应用]
症状：[具体表现]
根因：[查询慢 / 锁冲突 / GC / 网络抖动]
影响：[受影响场景]
建议：[索引 / 缓存 / 异步化 / 拆分 / 扩容]
```

---

## 7. 容量结论

```text
单实例容量：QPS = X
推荐实例数：N = 总 QPS / X * 1.5（预留 50% buffer）
水平扩展可行性：是 / 否
扩展瓶颈：[最先压垮的资源]
建议监控阈值：[CPU 80% / 错误率 1%]
```

---

## 8. 对比基线

| 版本 | P99 | QPS | 错误率 | 备注 |
|---|---|---|---|---|
| 上版本 |  |  |  |  |
| 本版本 |  |  |  | 退化 / 持平 / 改善 |
| 退化幅度 |  |  |  |  |

---

## 9. 风险和建议

```text
本次发现的性能风险：
1. [风险描述] - 建议：[处理方式]

建议监控告警：
1. [指标] > [阈值] 触发告警
```

---

## 10. 自检

```text
□ SLO 已与 PM/SRE 对齐
□ 测试数据规模接近生产
□ 测试场景覆盖主路径
□ 浸泡测试做了（≥ 1 小时）
□ 尖峰测试做了
□ 资源监控完整
□ 瓶颈已分析根因
□ 与上版本基线对比
□ 风险点已记录
□ 监控告警建议已给 SRE
```
