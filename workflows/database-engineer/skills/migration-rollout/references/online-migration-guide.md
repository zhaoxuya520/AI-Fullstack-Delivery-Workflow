# 在线迁移指南

## Expand / Contract 模式

```text
Expand：新增兼容结构，不破坏旧代码
Migrate：回填和双写，验证新旧一致
Contract：切换读取，清理旧结构
```

## 回填原则

- 分批执行，避免长事务。
- 记录进度，允许暂停和续跑。
- 幂等设计，重复执行不破坏数据。
- 配置限速，避免影响线上请求。

## 回滚原则

- 明确回滚触发条件。
- 先保留旧结构和旧数据，确认稳定后再清理。
- 不可逆操作必须单独评审。
- 回滚后要验证核心读写路径。
