# 索引设计指南

## 设计输入

- 查询频率
- 过滤条件
- 排序字段
- 分页方式
- join 关系
- 返回规模
- 写入频率

## 复合索引原则

- 高选择性过滤条件通常靠前。
- 等值过滤、范围过滤、排序字段的顺序要结合具体数据库优化器。
- 分页排序场景要同时考虑 where 和 order by。
- 唯一索引既是性能工具，也是数据质量约束。

## 反模式

- 为每个字段都建单列索引。
- 在低选择性字段上建立无效索引。
- 为低频后台查询牺牲核心写入链路。
- 没有执行计划验证就宣称优化完成。
