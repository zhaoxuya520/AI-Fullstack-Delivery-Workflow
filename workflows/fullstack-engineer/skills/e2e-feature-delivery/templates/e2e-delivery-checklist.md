# 端到端功能交付清单

## 1. 功能信息

```text
功能名：
PRD 链接：
验收标准：
技术栈：
预计工时：
负责人：
```

---

## 2. 垂直切片规划

| 切片 | 内容 | 预计时间 | 状态 |
|---|---|---|---|
| 1 | 最小可用（Schema + CRUD + 列表页） |  | ⏳ |
| 2 | 详情 + 表单 + 联调 |  | ⏳ |
| 3 | 错误处理 + 权限 + 测试 |  | ⏳ |

---

## 3. 数据层

```text
□ Schema 设计完成
□ Migration 文件创建
□ 种子数据（如需）
□ 索引覆盖核心查询
□ 关系正确（FK / 唯一约束）
```

---

## 4. 后端层

```text
□ API 端点清单：
  - [ ] POST /xxx（创建）
  - [ ] GET /xxx（列表）
  - [ ] GET /xxx/:id（详情）
  - [ ] PATCH /xxx/:id（更新）
  - [ ] DELETE /xxx/:id（删除）
□ 入参校验（Zod / class-validator）
□ 错误码统一
□ 权限检查
□ 幂等（写操作）
□ 分页 / 筛选 / 排序
```

---

## 5. 前端层

```text
□ 页面清单：
  - [ ] 列表页
  - [ ] 详情页
  - [ ] 创建 / 编辑表单
□ Loading 状态
□ Error 状态
□ Empty 状态
□ 表单校验
□ 响应式（移动端）
□ 基本 a11y（键盘 + 对比度）
```

---

## 6. 联调层

```text
□ 前后端字段名一致
□ 错误码 → 前端提示映射
□ Token 传递正确
□ 分页参数对接
□ 文件上传（如有）
□ 实时更新（如有）
```

---

## 7. 认证层

```text
□ 注册 / 登录实现
□ Token 存储（HttpOnly Cookie / localStorage）
□ 前端路由守卫
□ 后端中间件
□ 权限检查（角色 / 资源归属）
```

---

## 8. 测试层

```text
□ 核心业务逻辑单元测试
□ API 集成测试（至少主路径）
□ E2E 关键路径（至少 1 条）
□ 表单校验测试
```

---

## 9. 部署层

```text
□ Preview 环境部署成功
□ 环境变量配置
□ 数据库连接正常
□ 域名 / HTTPS
□ 健康检查
```

---

## 10. 验收

```text
□ 验收标准 1：
□ 验收标准 2：
□ 验收标准 3：
□ 移动端可用
□ 性能可接受（首屏 < 3s）
```

---

## 11. 自检

```text
□ 垂直切片交付
□ Schema First
□ 边写边调
□ 4 种状态
□ 错误处理端到端
□ 认证端到端
□ 测试覆盖
□ Preview 可访问
□ 验收通过
```
