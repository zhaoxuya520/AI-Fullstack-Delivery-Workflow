# 分页筛选排序模板

## 1. 适用端点

| Method | Path | 是否分页 | 是否筛选 | 是否排序 | 是否搜索 |
|---|---|---|---|---|---|
| GET |  | 是 | 是 | 是 | 是 |

---

## 2. 分页规则

```text
分页模式：offset / page / cursor
默认页大小：
最大页大小：
第一页规则：
下一页标识：
总数是否返回：
```

示例：

```text
?page=1&pageSize=20
?limit=20&cursor=xxx
```

---

## 3. 筛选规则

| 字段 | 类型 | 操作符 | 示例 | 说明 |
|---|---|---|---|---|
| status | enum | eq/in | `status=active` |  |

---

## 4. 排序规则

| 字段 | 默认方向 | 是否允许多字段 | 示例 |
|---|---|---|---|
| createdAt | desc | 否 | `sort=-createdAt` |

---

## 5. 搜索规则

```text
搜索参数：
搜索字段范围：
大小写规则：
模糊匹配规则：
无结果响应：
```

---

## 6. 字段选择和展开

```text
fields：
include/expand：
默认返回字段：
禁止返回字段：
```
